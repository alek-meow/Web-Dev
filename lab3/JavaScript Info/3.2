function pow(x,n)                       нет пробела между параметрами функции
{                                       фигурная скобка на отдельной строке
  let result=1;                         нет пробелов сбоку от оператора равно
  for(let i=0;i<n;i++) {result*=x;}     нет пробелов вокруг операторов, фигурные скобки либо убрать, либо перенести на другую строку
  return result;
}

let x=prompt("x?",''), n=prompt("n?",'')            опять нет пробелов, нет ; в конце сторки, лучше разделить на две строки
if (n<=0)                                           п р о б е л ы, было б неплохо оставить сверху пустую строку
{                                                   фигурная скобка на отдельной строке
  alert(`Степень ${n} не поддерживается, введите целую степень, большую 0`);        длинная строка
}                                                   фигурная скобка на отдельной строке
else                                                лучше написать } else {
{
  alert(pow(x,n))                                   вокруг вложенного вызова нужны пробелы, нет ;
}